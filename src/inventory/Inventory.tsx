// import styled from "styled-components";
import React, { useState, useEffect } from "react";

import { FantList, FantListItem } from "./Components.compot.yml";
// import C from "./Components.compot.yml";

// console.log(C, typeof C, C.FanList);

const Inventory = () => {
  // return <FantList />;
  return <div>HHH</div>;
};

// const FantList = styled.ul`
//   list-style: none;
// `;

// const FantListItem = styled.li``;

export default Inventory;

/*
Item consist of:
  - source (facebook, mail, google, yt)
  - name (Will Smith, NASA, neighbour)
  - avatar
  - media + metadata (duration, format, size, ...)
  - summary
  - actions:
    - mark as read
    - remind me
    - archive & delete
    - refant (retweet)
    - add reactions
    - add comment
    - custom actions (poll, chat bot, events)
     

*/
